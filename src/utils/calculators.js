export default {
  divideNumber: (num1, num2) => {
    if (num2 === 0) return 0
    return num1 / num2
  },
  plusNumber: (num1, num2) => {
    return num1 + num2
  }
}

// https://github.com/noomerzx/vue-example-proj.git